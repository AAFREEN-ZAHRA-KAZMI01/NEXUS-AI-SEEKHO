[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks tests that call real external services (Gemini, URLs)
    unit: marks pure unit tests with no external dependencies
addopts = -v --tb=short
