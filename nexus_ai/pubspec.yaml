name: nexus_ai
description: Nexus AI — Autonomous Content-to-Action Intelligence
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  dio: ^5.4.3
  google_fonts: ^6.2.1
  file_picker: ^8.0.3
  flutter_animate: ^4.5.0
  shared_preferences: ^2.2.3
  url_launcher: ^6.3.0
  shimmer: ^3.0.0
  percent_indicator: ^4.2.3
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
